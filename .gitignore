runs/
__pycache__/
*.pyc
caption_caches/
visual_indexes/
query_caches/
.pytest_cache/
*.egg-info/
.env

# Decoded frames and DVD per-video scratch state. Regenerated from the videos.
vendor/dvd_stack/dvd/run_workspace/

# Datasets are synced separately, never committed.
data/

# Local Hugging Face hub mirror (~5G). Re-downloaded, never committed.
hub_data_kt/

# Run logs. The launchers, patches and as-run snapshots beside them are kept.
logs/*.log
